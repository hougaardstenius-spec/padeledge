def classify_strokes(video_file):
    """
    This is a placeholder function for testing.
    Later, we’ll add real AI logic to detect padel strokes.
    """
    # For now, just simulate a result
    return {
        "forehands": 10,
        "backhands": 8,
        "volleys": 5,
        "smashes": 2,
        "status": "Analysis complete (test mode)"
    }
